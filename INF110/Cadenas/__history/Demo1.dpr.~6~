program Demo1;
uses Cadena, math, sysutils;

var
    cad : string;
    cantidad : byte;
begin
  write('Ingrese la cadena');
  readln(cad);
  cantidad := ObtenerCantidadDeNumeros(cad);
  writeln(cantidad);
  readln;
end.
